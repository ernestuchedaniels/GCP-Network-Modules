name: Terraform GitOps Pipeline

on:
  workflow_dispatch:  # Manual trigger for testing
  push:
    branches: [main]
    paths:
      - 'dev-root/**'
      - 'prod-root/**'
  pull_request:
    branches: [main]
    paths:
      - 'dev-root/**'
      - 'prod-root/**'

env:
  TF_CLOUD_ORGANIZATION: "Visa-replica"
  TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      changed-workspaces: ${{ steps.changes.outputs.workspaces }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed workspaces
        id: changes
        run: |
          # Get changed files
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }})
          else
            CHANGED_FILES=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }})
          fi
          
          # Extract workspace names from changed paths
          WORKSPACES=$(echo "$CHANGED_FILES" | grep -E '^(dev|prod)-root/' | cut -d'/' -f1-2 | sort -u | while read path; do
            if [ -n "$path" ]; then
              env=$(echo $path | cut -d'-' -f1)
              stage=$(echo $path | cut -d'/' -f2)
              echo "${env}-${stage}"
            fi
          done | jq -R -s -c 'split("\n")[:-1]')
          
          echo "workspaces=$WORKSPACES" >> $GITHUB_OUTPUT
          echo "Changed workspaces: $WORKSPACES"



  speculative-plan:
    needs: [detect-changes]
    if: github.event_name == 'pull_request' && needs.detect-changes.outputs.changed-workspaces != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        workspace: ${{ fromJson(needs.detect-changes.outputs.changed-workspaces) }}
    steps:
      - uses: actions/checkout@v4
      - name: Trigger Speculative Plan
        id: trigger-plan
        run: |
          # Get workspace ID
          WORKSPACE_ID=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/Visa-replica/workspaces/${{ matrix.workspace }}" | \
            jq -r '.data.id')
          
          # Trigger speculative plan
          RUN_RESPONSE=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            --request POST \
            --data "{
              \"data\": {
                \"attributes\": {
                  \"is-destroy\": false,
                  \"message\": \"Speculative plan for PR #${{ github.event.number }}\"
                },
                \"type\": \"runs\",
                \"relationships\": {
                  \"workspace\": {
                    \"data\": {
                      \"type\": \"workspaces\",
                      \"id\": \"$WORKSPACE_ID\"
                    }
                  }
                }
              }
            }" \
            https://app.terraform.io/api/v2/runs)
          
          RUN_ID=$(echo "$RUN_RESPONSE" | jq -r '.data.id')
          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
          echo "workspace_id=$WORKSPACE_ID" >> $GITHUB_OUTPUT
          echo "Triggered run: $RUN_ID"

      - name: Wait for Plan and Get Results
        run: |
          RUN_ID="${{ steps.trigger-plan.outputs.run_id }}"
          
          # Wait for plan to complete (max 5 minutes)
          for i in {1..30}; do
            RUN_STATUS=$(curl -s \
              --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
              --header "Content-Type: application/vnd.api+json" \
              "https://app.terraform.io/api/v2/runs/$RUN_ID" | \
              jq -r '.data.attributes.status')
            
            echo "Run status: $RUN_STATUS"
            
            if [[ "$RUN_STATUS" == "planned" || "$RUN_STATUS" == "errored" || "$RUN_STATUS" == "canceled" ]]; then
              break
            fi
            
            sleep 10
          done
          
          # Get plan details
          PLAN_ID=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/runs/$RUN_ID" | \
            jq -r '.data.relationships.plan.data.id')
          
          if [ "$PLAN_ID" != "null" ]; then
            PLAN_DETAILS=$(curl -s \
              --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
              --header "Content-Type: application/vnd.api+json" \
              "https://app.terraform.io/api/v2/plans/$PLAN_ID")
            
            RESOURCE_ADDITIONS=$(echo "$PLAN_DETAILS" | jq -r '.data.attributes."resource-additions"')
            RESOURCE_CHANGES=$(echo "$PLAN_DETAILS" | jq -r '.data.attributes."resource-changes"')
            RESOURCE_DESTRUCTIONS=$(echo "$PLAN_DETAILS" | jq -r '.data.attributes."resource-destructions"')
            
            echo "📋 **Plan Summary for \`${{ matrix.workspace }}\`**" >> plan_summary.md
            echo "" >> plan_summary.md
            echo "- **Resources to add:** $RESOURCE_ADDITIONS" >> plan_summary.md
            echo "- **Resources to change:** $RESOURCE_CHANGES" >> plan_summary.md
            echo "- **Resources to destroy:** $RESOURCE_DESTRUCTIONS" >> plan_summary.md
            echo "- **Status:** $RUN_STATUS" >> plan_summary.md
            echo "" >> plan_summary.md
            echo "🔗 [View detailed plan in TFE](https://app.terraform.io/app/Visa-replica/workspaces/${{ matrix.workspace }}/runs)" >> plan_summary.md
            
            cat plan_summary.md
          fi

      - name: Display Plan Results
        run: |
          if [ -f "plan_summary.md" ]; then
            echo "=== TERRAFORM PLAN SUMMARY ==="
            cat plan_summary.md
            echo "=============================="
          else
            echo "No plan summary available"
          fi

  apply-on-merge:
    needs: [detect-changes]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.detect-changes.outputs.changed-workspaces != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        workspace: ${{ fromJson(needs.detect-changes.outputs.changed-workspaces) }}
    steps:
      - name: Trigger Apply Run
        run: |
          # Get workspace ID
          WORKSPACE_ID=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/Visa-replica/workspaces/${{ matrix.workspace }}" | \
            jq -r '.data.id')
          
          # Trigger apply run
          curl \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            --request POST \
            --data "{
              \"data\": {
                \"attributes\": {
                  \"is-destroy\": false,
                  \"message\": \"Apply run triggered by merge to main - ${{ github.sha }}\"
                },
                \"type\": \"runs\",
                \"relationships\": {
                  \"workspace\": {
                    \"data\": {
                      \"type\": \"workspaces\",
                      \"id\": \"$WORKSPACE_ID\"
                    }
                  }
                }
              }
            }" \
            https://app.terraform.io/api/v2/runs